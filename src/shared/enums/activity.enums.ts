/* ======================================================
 * ENUMS
 * ====================================================== */

export enum ActivityStatus {
  ACTIVE = 'ACTIVE',
  COMPLETED = 'COMPLETED',
  PAUSED = 'PAUSED',
}
