/* ======================================================
 * ENUMS
 * ====================================================== */

export enum WorkSessionStatus {
  ACTIVE = 'ACTIVE',
  COMPLETED = 'COMPLETED',
  PAUSED = 'PAUSED',
}
